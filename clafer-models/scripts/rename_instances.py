from sys import argv
from os import listdir
def renamez(dir, counter):
    import pdb; pdb.set_trace()
    #for thing in listdir(dir):
        #file.replace(file.split('.')[0], file.split('.')[0]counter)
renamez(argv[1], argv[2])
